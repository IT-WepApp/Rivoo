name: admin_panell # Corrected name
description: تطبيق لوحة إدارة النظام.
publish_to: "none"
version: 1.0.0+1
environment:
  sdk: ">=2.17.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  # Added missing dependencies
  flutter_riverpod: ^2.4.10
  go_router: ^13.2.0
  intl: ^0.19.0
  fl_chart: ^0.65.0
  firebase_core: ^3.13.0
  firebase_auth: ^5.5.2
  # Added missing Firebase dependencies
  firebase_crashlytics: ^3.4.8
  # Added missing test dependencies
  mockito: ^5.4.4
  # End of added dependencies
  shared_models:
    path: ../shared_modules/shared_models
  shared_services:
    path: ../shared_modules/shared_services
  shared_widgets:
    path: ../shared_modules/shared_widgets
  shared:
    path: ../shared
  shared_core:
    path: ../shared_core
  flutter_secure_storage: ^9.0.0
  cloud_firestore: ^5.6.6
  equatable: ^2.0.5
  cloud_functions: any
dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.8
flutter:
  uses-material-design: true
